#include "push_swap.h"

void less_then_three(t_push_swap ps_a)
{
    if (!(sort_complete()))
    {
        rra_no_write(ps_a->stack);
        if (!(sort_complete()))
            ra_no_write(ps_a);
        ra_no_write(ps_a);
        if (!(sort_complete()))
            rra_no_write(ps_a);
        if(!(wort_complete()))
        {
            sa_no_write(ps_a);
            rra_no_write(ps_a);
        if (!(sort_complete()))
            ra_no_write(ps_a);
        ra_no_write(ps_a);
        if (!(sort_complete()))
            rra_no_write(ps_a);
        }
    }
}